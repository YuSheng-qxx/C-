//https://www.nowcoder.com/practice/68ae3b28b4a04e098485c4423e2c6548?tpId=107&tags=&title=&diffculty=0&judgeStatus=0&rp=1
#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main()
{
    int n;
    scanf("%d",&n);
    int a[n];
    vector<int> v;
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        v.push_back(a[i]);
    }
    int num;
    scanf("%d",&num);
    v.push_back(num);
    sort(v.begin(), v.end());
    for(auto n:v)
        cout<<n<<" ";
    cout<<endl;
    return 0;
}
